<manifest xmlns:tools="http://schemas.android.com/tools"
    package="com.face.face"
    xmlns:android="http://schemas.android.com/apk/res/android">
 <uses-permission android:name="android.permission.CAMERA"/>
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

<uses-feature android:name="android.hardware.camera.front" android:required="true"/>
<uses-feature android:name="android.hardware.camera" android:required="true"/>

    <application
        android:label="Face.ing"
        android:name=".globle.MainApplication"
        android:supportsRtl="true"
        android:allowBackup="true"
        tools:replace="android:label"
        android:icon="@mipmap/ic_launcher"
         android:enableOnBackInvokedCallback="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:ignore="MissingClass">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoTitleBar"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            tools:ignore="MissingClass">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ✅ Corrected AnuraExampleMeasurementActivity Declaration -->
        <activity
            android:name=".measurement.FaceAnuraMeasurementActivity"
            android:theme="@style/Theme.AppCompat.Light"
            android:exported="true" />
        <activity
            android:name=".measurement.FaceStartActivity"
            android:theme="@style/Theme.AppCompat.Light"
            android:exported="true" />
             <activity
            android:name=".measurement.FaceResultsActivity"
            android:theme="@style/Theme.AppCompat.Light"
            android:exported="true" />
             <activity
            android:name=".ui.HealthResultActivity"
            android:theme="@style/Theme.AppCompat.Light"
            android:exported="true" />
            <activity
            android:name="io.flutter.embedding.android.FlutterActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
